"use client";

const AddToCart = () => {
  return (
    <div>
      <button
        className="btn btn-primary"
        onClick={() => console.log("CLICKED!")}
      >
        Add to Cart
      </button>
    </div>
  );
};
export default AddToCart;
